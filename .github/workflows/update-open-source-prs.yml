name: Update Open Source Merged PR List

on:
  schedule:
    - cron: '17 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-merged-prs:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Update README merged PR section
        id: update
        uses: actions/github-script@v7
        env:
          GH_USERNAME: wwenrr
        with:
          script: |
            const run = require('./.github/scripts/update-open-source-prs.js');
            await run({ github, core, context });

      - name: Commit changes
        run: |
          if git diff --quiet README.md; then
            echo "No README changes."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "chore(profile): update open source merged PR list"
          git push
